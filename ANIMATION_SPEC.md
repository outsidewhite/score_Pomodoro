# 旅アニメーション仕様案

## 1. 目的

集中タイマーと1分ごとの獲得スコアを、白黒のピクトグラム風アニメーションとして表示します。

旅は「日本 → 世界 → 宇宙 → 月面」の4ステージで進行し、月面到着後は獲得スコアに応じて探索レベルを継続的に上げます。

本書では、現在の採点仕様である1分区間と獲得スコア`0 | 1 | 2 | 3`を前提とします。

## 2. 表示に使用する値

ステージと乗り物は異なる値から決定します。

| 表示要素 | 使用する値 |
|---|---|
| 現在のステージ | セッションの累積獲得スコア |
| 次の目的地までの進捗 | セッションの累積獲得スコア |
| 計測中の乗り物 | 直近の完了区間の獲得スコア |
| 休憩・離席表示 | 現在のタイマーモード |
| 月面の探索レベル | 月面到着後に獲得した累積スコア |

累積獲得スコアを乗り物判定へ直接使用しません。乗り物は直近の1分区間の状態を表し、ステージはセッション全体の進行を表します。

## 3. 旅ステージ

既存ルートの45点間隔に合わせ、ステージ境界を次のように定義します。

| 累積獲得スコア | ステージ | 内容 |
|---:|---|---|
| 0～359 | 日本 | 日本国内の目的地を巡る |
| 360～899 | 世界 | 世界各地の目的地を巡る |
| 900～1,079 | 宇宙 | 地球周回軌道から月へ向かう |
| 1,080以上 | 月面 | 月面を探索してレベルを上げる |

1つの目的地または探索レベルに必要なスコアは45点です。

最大の3点を毎分獲得した場合の目安は次のとおりです。

| 到達地点 | 必要時間の目安 |
|---|---:|
| 世界ステージ | 2時間 |
| 宇宙ステージ | 5時間 |
| 月面ステージ | 6時間 |

## 4. 月面探索レベル

### 4.1 開始条件

累積獲得スコアが1,080点へ到達した時点で、月面ステージの探索レベル1を開始します。

### 4.2 計算式

```text
月面探索スコア = max(0, 累積獲得スコア - 1,080)

探索レベル = floor(月面探索スコア ÷ 45) + 1

現在レベル内の進捗 = 月面探索スコア mod 45

次レベルまでの残り = 45 - 現在レベル内の進捗
```

探索レベルには上限を設けず、セッション継続中は上がり続けます。

### 4.3 境界例

| 累積獲得スコア | 探索レベル | レベル内進捗 |
|---:|---:|---:|
| 1,080 | 1 | 0 / 45 |
| 1,124 | 1 | 44 / 45 |
| 1,125 | 2 | 0 / 45 |
| 1,170 | 3 | 0 / 45 |

### 4.4 月面での表示

- 「探索レベル N」を常に表示します。
- 次のレベルまでの残りスコアを表示します。
- 45点ごとに探索対象を切り替えます。
- レベルアップ時だけ短い発見演出を表示します。
- 累積スコアが減らない限り、探索レベルも下がりません。

探索対象はレベル番号を循環利用しても構いません。

```text
クレーター → 岩石 → 洞窟 → 氷 → 観測地点 → クレーター…
```

## 5. アニメーション状態

```ts
type JourneyStage = 'japan' | 'world' | 'space' | 'moon'

type MotionState =
  | 'idle'
  | 'preparing'
  | 'score0'
  | 'score1'
  | 'score2'
  | 'score3'
  | 'break'
  | 'away'
```

### 5.1 状態の優先順位

複数の条件が同時に成立した場合は、次の順序で表示を決定します。

1. カメラ・モデル準備中：`preparing`
2. セッション開始前：`idle`
3. 離席・タイマー停止中：`away`
4. 休憩中：`break`
5. 集中開始後で採点前：`preparing`
6. 集中計測中：直近区間の`score0`～`score3`

## 6. ステージ別の表示

| 状態 | 日本 | 世界 | 宇宙 | 月面 |
|---|---|---|---|---|
| 開始前 | 家 | 空港・ホテル | 宇宙ステーション | 月面基地 |
| 採点待ち | 出発準備 | 搭乗準備 | 発射準備 | 探索準備 |
| 0点 | 立ち止まる | 乗り物が停車 | 機体が停止 | 探索を停止 |
| 1点 | 徒歩 | 自転車 | 宇宙遊泳 | 宇宙飛行士 |
| 2点 | 車 | 電車 | スペースシャトル | 月面車 |
| 3点 | ヘリコプター | 飛行機 | ロケット | 月着陸船 |
| 休憩 | ベンチで休憩 | 待合室で休憩 | 休憩ポッド | 基地内で休憩 |
| 離席 | 車両点検 | 乗り物を駐機 | 機体点検 | 月面車を整備 |

離席を「けが」として表現すると失敗感が強くなるため、初期実装では点検・整備として表現します。

## 7. 動きの方針

### 7.1 共通

- 白背景・黒線を基本とするピクトグラム風SVGで構成します。
- 色だけに依存せず、状態名をテキストでも表示します。
- スコア確定時だけ乗り物を切り替えます。
- 休憩・離席中は背景スクロールと進行アニメーションを停止します。
- ステージ到着と探索レベルアップ時だけ、短い強調演出を再生します。
- 集中を妨げないよう、自動再生音は使用しません。

### 7.2 乗り物

| 乗り物 | 動き |
|---|---|
| 徒歩・宇宙飛行士 | 腕と脚を小さく交互に動かす |
| 自転車・車・電車・月面車 | 車輪を回転し、車体を上下に1～2px動かす |
| ヘリコプター | ローター回転と小さな上下移動 |
| 飛行機・シャトル | 水平方向の移動と小さな浮遊 |
| ロケット・月着陸船 | 噴射部分の伸縮と緩やかな上昇 |

1点・2点・3点の差は、過度な速度差ではなく乗り物の種類と背景の移動量で表現します。

## 8. 状態遷移

```text
計測画面へ遷移
  ↓
カメラ・モデル準備中
  ↓
準備完了／タイマー開始可能
  ↓
集中開始／採点待ち
  ↓
1分区間完了
  ↓
獲得スコアに応じて乗り物更新
```

- 休憩・離席では累積スコアを増やしません。
- 集中へ戻った場合、直近の乗り物から再開準備を経て計測を再開します。
- 未完了の1分区間は乗り物変更へ使用しません。
- 複数のステージ境界を一度に越えた場合も、最終的な累積値から現在ステージを決定します。

## 9. 保存と再読み込み

以下を採点セッションIDへ紐づけて保存します。

- 選択された日本ルート
- 選択された世界ルート
- 累積獲得スコアの算出元となる完了区間
- 直近の完了区間ID
- 直近の獲得スコア

現在ステージ、目的地、月面探索レベルは累積獲得スコアから再計算できるため、重複保存しません。

再読み込み後も同じルート、ステージ、乗り物、探索レベルを復元します。進行途中のCSSアニメーション時刻は復元せず、現在状態の先頭フレームから再開します。

## 10. アクセシビリティ

- `prefers-reduced-motion: reduce`では移動・回転・点滅を停止します。
- 停止時も現在地、状態、探索レベル、残りスコアをテキスト表示します。
- 1分区間の確定、ステージ到着、探索レベルアップだけを`aria-live`で通知します。
- 秒単位の背景移動は読み上げ対象にしません。
- 装飾用SVGは`aria-hidden="true"`とします。

## 11. 実装単位

| 実装 | 役割 |
|---|---|
| 旅進行ロジック | 累積値からステージ・目的地・探索レベルを計算 |
| 状態変換ロジック | タイマー状態と直近獲得点から`MotionState`を計算 |
| ステージ設定 | 背景、乗り物、ラベル、境界値を定義 |
| SVG部品 | 人物、乗り物、背景、小物を描画 |
| アニメーション制御 | 状態変更・到着・レベルアップを再生 |
| 保存処理 | 選択ルートと直近区間をセッションへ保存 |

ステージや状態の判定は純粋関数として実装し、Reactコンポーネント内へ境界値を直接記述しません。

## 12. 最低限のテスト項目

- 359点は日本、360点は世界になること
- 899点は世界、900点は宇宙になること
- 1,079点は宇宙、1,080点は月面になること
- 1,124点は探索レベル1、1,125点はレベル2になること
- タイマー状態が乗り物状態より優先されること
- 休憩・離席で累積スコアが増えないこと
- 未完了区間で乗り物が変わらないこと
- 再描画で到着・レベルアップ演出を二重再生しないこと
- 再読み込み後も選択ルートと探索レベルが変わらないこと
- モーション軽減設定でアニメーションが停止すること

## 13. 現在の実装との差分

- 現在の`RouteArea`は日本・世界・宇宙のみで、月面がありません。
- 現在の`Journey`には月面ルートがありません。
- 宇宙ルートの最終地点は1,035点の「月周回軌道」です。
- 1,080点以降の探索レベル計算は未実装です。
- 選択ルートはメモリ上にあり、再読み込み時に同じルートを保証していません。
- 現在の表示は進捗点と簡易トラベラーのみで、タイマーモードと直近獲得スコアを受け取っていません。

この差分を旅ロジック、状態判定、静止SVG、CSSアニメーションの順に実装します。
