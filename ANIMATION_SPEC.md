# 旅アニメーション仕様案

## 1. コンセプト

集中して獲得したスコアを、白黒のピクトグラム風アニメーションによる「旅」として表示します。

旅は準備ステージを経て、次の3ステージで進行します。

```text
日本ステージ → 世界ステージ → 宇宙ステージ
```

準備中は累積スコアによる移動ステージへ属さず、常に自宅の静止シーンを表示します。

準備はSTARTから計測画面へ移動して初めてタイマーを開始するまでです。一度開始した後の停止・カメラ再取得・モデル再読み込みでは準備へ戻りません。開始直後から最初の採点結果までは1点の移動アニメーションを使用します。

### 旅の振り返り（Issue #50）

- 旅表示右上の丸い地図／リストボタンから、同じ枠内に履歴を重ねて表示します。左上の×またはEscapeで閉じ、入口へフォーカスを戻します。
- 地図は最初の目的地に到着してから利用できます。リストは計測画面の準備中から利用できます。集中中は両方とも無効で、集中へ戻ると開いていた履歴も閉じます。
- 地図は到達したステージだけを表示します。日本は30点、世界は240点、宇宙は600点で解放され、次の目的地として予告されている段階では地図を表示しません。到達済みのステージの地図は引き続き閲覧できます。
- 日本・世界の地形は同梱したNatural Earthデータを使い、目的地を同じ投影の緯度・経度に配置します。世界の両ルートに対応します。位置が未定義の「スタート」は地図に置かず、地図上の番号に対応する一覧へ地名・到達状態・到着時刻を表示します。線は訪問順を示し、道路や実際の飛行経路ではありません。宇宙は距離・縮尺を再現しない模式図と明記します。
- 日本・世界の表示範囲は公開済みの地点と番号に合わせて余白を詰め、進行に合わせて広げます。地理座標は維持し、過度な拡大を防ぐ最小範囲を設けます。地図は縦横比を保って高さ300px以内に収めます。
- `JourneyMap`と`JourneyList`は独立したコンポーネントです。`JourneyReview`が開閉を担い、既存の`ScoreJourney`を同じ枠に配置します。
- 地図は通過済み・現在地を黒丸と実線、次の目的地を白丸と点線で表示します。日本・世界ではそれより先の丸を表示しません。宇宙は固定した丸をすべて残し、次の目的地になるまで地名を`???`にします。
- リストは実在する最終目的地までを並べ、次の次以降の名前を`???`にします。探索レベル用の行は追加しません。到達済みは黒、未到達は灰色です。
- 到着時刻は到着時のメインタイマー表示（累積作業時間、`HH:MM:SS`）を秒単位で保存します。地図とリストは同じ記録を参照し、リストの実測値だけ緑色にします。未到達の参考時刻は最後の到着から15分刻みで計算します。
- 到着履歴はセッションIDと選択ルートを照合して復元します。履歴のない旧セッションの実測時刻は推測せず`—`と表示します。
- 履歴内はスクロール可能です。狭い画面ではページも縦スクロールできるようにし、地図の文字を縮小しすぎない幅を維持します。動きを減らす設定では展開・現在地のアニメーションを止めます。

- 日本で身近な移動から旅を始める
- 世界へ飛び出し、移動スケールが広がる
- 宇宙へ到達した後は、観測ミッションを続ける

月面は独立したステージにせず、月面探索や探索レベルも設けません。月は宇宙ステージ内で通過・観測する目的地の1つとして扱います。

本書では、現在の採点仕様である1分区間と獲得スコア`0 | 1 | 2 | 3`を前提とします。

## 2. 体験設計の方針

アニメーションの目的は、集中度を細かく監視させることではなく、「作業を続けた結果、少しずつ遠くへ進んだ」と感じられることです。

- 1分ごとの結果は、乗り物の変更ではなく動きの強弱で表現する
- ステージごとに主役となる乗り物を固定し、旅の連続性を保つ
- 通常時は静かに動かし、到着時だけ短い強調演出を入れる
- 低いスコアや離席を失敗として責める表現にしない
- 宇宙到達後もスコアを蓄積でき、旅が行き止まりにならないようにする

## 3. 表示に使用する値

| 表示要素 | 使用する値 |
|---|---|
| 現在のステージ | セッションの累積獲得スコア |
| 現在地・次の目的地 | セッションの累積獲得スコア |
| 乗り物の動き | 直近の完了区間の獲得スコア |
| 休憩・離席表示 | 現在のタイマーモード |
| 宇宙ミッション番号 | 宇宙ステージ到達後の累積獲得スコア |

ステージはセッション全体の成果、動きは直近1分の状態を表します。累積獲得スコアを動きの強弱へ直接使用しません。

## 4. 旅ステージ

各目的地の間隔を30点とし、ステージ境界を次のように定義します。

| 累積獲得スコア | ステージ | 主役 | 内容 |
|---:|---|---|---|
| 0～239 | 日本 | 車 | 国内の目的地を巡る |
| 240～599 | 世界 | 飛行機 | 世界各地の目的地を巡る |
| 600以上 | 宇宙 | 宇宙船 | 地球を離れ、観測ミッションを続ける |

1つの目的地またはミッションに必要なスコアは30点です。

最大の3点を毎分獲得した場合、世界ステージへは約1時間20分、宇宙ステージへは約3時間20分で到達します。宇宙ステージには終端を設けません。

## 5. ステージごとの物語

### 5.1 日本ステージ

日常から旅が始まる導入ステージです。車を主役とし、街並みや地域のランドマークが車窓を流れます。

- 開始地点：自宅近くの駅
- 目的地例：福岡、広島、大阪、京都、名古屋、東京、札幌
- 到着演出：目的地の標識が現れ、車が短く停車する
- 世界への遷移：終着駅から空港へ移動し、飛行機が離陸する

### 5.2 世界ステージ

旅の広がりを感じる中盤ステージです。飛行機を主役とし、雲や都市のシルエットで地域の変化を表現します。

- 開始地点：日本の空港
- 目的地例：ソウル、北京、バンコク、シンガポール、ドバイ、ローマ、パリ、ロンドン、ニューヨーク
- 到着演出：都市のランドマークと都市名を短く表示する
- 宇宙への遷移：空港の滑走路が発射施設へつながり、宇宙船が上昇する

### 5.3 宇宙ステージ

旅の最終ステージであり、到達後も継続できる長期ステージです。宇宙船を主役とし、30点ごとに新しい観測地点へ進みます。

初回の観測ルートは次の順序とします。

| 必要スコア | 観測地点 |
|---:|---|
| 600 | 宇宙ステーション |
| 630 | 地球周回軌道 |
| 660 | 月への航路 |
| 690 | 月周回軌道 |

月では着陸せず、月面探索へも移行しません。

最後の目的地である月周回軌道へ到着した後は、30点ごとに「探索レベル N」を更新します。探索レベルが上がる際は、目的地への到着演出を再生しません。

```text
探索スコア = max(0, 累積獲得スコア - 690)

探索レベル = floor(探索スコア ÷ 30) + 1

現在レベル内の進捗 = 探索スコア mod 30
```

## 6. アニメーション状態

```ts
type JourneyStage = 'japan' | 'world' | 'space'

type MotionState =
  | 'idle'
  | 'preparing'
  | 'score0'
  | 'score1'
  | 'score2'
  | 'score3'
  | 'break'
```

### 6.1 状態の優先順位

複数の条件が同時に成立した場合は、次の順序で表示を決定します。

1. カメラ・モデル準備中：`preparing`
2. セッション開始前：`idle`
3. 離席・タイマー停止中：`idle`
4. 休憩中：`break`
5. 集中開始後で最初の採点前：`preparing`
6. 集中計測中：直近区間の`score0`～`score3`

## 7. 状態別の見せ方

乗り物はステージ内で固定し、獲得スコアによって次の動きへ切り替えます。

| 状態 | 動き | 背景 | 表示文言例 |
|---|---|---|---|
| 開始前・離席 | ステージ別の停止シーン | 静止 | 停止中 |
| 採点待ち・準備中 | 自宅の準備シーン | 静止 | 作業を始める準備中 |
| 0点 | 安全に停止 | 静止 | ひと休み |
| 1点 | ゆっくり進む | 低速 | ゆっくり前進中 |
| 2点 | 安定して進む | 標準 | 順調に移動中 |
| 3点 | 少し力強く進む | 標準より少し速い | 快調に移動中 |
| 休憩 | ステージ別の休憩シーン | 静止 | 休憩中 |

1点から3点の差は小さくし、ユーザーがアニメーションを監視したくなるほど大きな速度差や派手な演出は付けません。

### 7.1 ステージ固有の動き

| ステージ | 通常移動 | 停止・休憩 | 背景要素 |
|---|---|---|---|
| 日本 | 車輪と街並みが流れる | 停止はコンビニ、休憩はホテル | 街、道路、目的地の標識 |
| 世界 | 小さな浮遊と雲が流れる | 停止は空港、休憩は空港ラウンジ | 雲、海、都市シルエット |
| 宇宙 | 噴射の伸縮と星が流れる | 停止は宇宙空間を漂い、休憩は宇宙ポッド室内 | 地球、星、惑星、観測対象 |

## 8. 到着とステージ遷移

通常の1分更新と、目的地到着・ステージ遷移を分けて扱います。

### 8.1 目的地到着

- 30点の境界を越えた直後に1.5～2秒だけ再生する
- 目的地名を表示し、背景のランドマークを切り替える
- 紙吹雪や点滅は使わず、線が広がる・旗が立つ程度に抑える
- 同じ到着イベントは再描画や再読み込みで二重再生しない
- 最後の目的地以降の探索レベル更新では到着演出を再生しない

### 8.2 ステージ遷移

ステージ遷移は旅の節目として、通常の到着より少し長い3～4秒の演出にします。

```text
日本 → 世界
目的地へ到着 → 空港の案内表示 → 飛行機が離陸

世界 → 宇宙
飛行機が着陸 → 発射施設へ切り替え → 宇宙船が上昇 → 地球が見える
```

演出中もタイマーと採点は止めません。演出をスキップしても、現在ステージとスコアには影響しません。

## 9. 状態遷移

```text
計測画面へ遷移
  ↓
カメラ・モデル準備中
  ↓
準備完了／タイマー開始可能
  ↓
集中開始／最初の採点待ち
  ↓
1分区間完了
  ↓
獲得スコアを加算し、動き・現在地を更新
  ↓
境界を越えた場合だけ到着・ステージ遷移を再生
```

- 休憩・離席では累積スコアを増やしません。
- 集中へ戻る場合は待機状態を経て、新しい1分区間を開始します。
- 未完了の1分区間は動きや旅の進行へ使用しません。
- 複数の境界を一度に越えた場合は、最終的な累積値から現在地を決定し、最後に越えた主要イベントだけを表示します。

## 10. 表示レイアウト

アニメーション領域は、情報を次の3層に分けます。

```text
┌──────────────────────────────┐
│ 日本 / 世界 / 宇宙       累積 540点 │  状態・成果
│                              │
│       背景 + 乗り物 + 目的地       │  アニメーション
│                              │
│ シンガポール ━━━━━━━ ドバイ       │  現在地・進捗
│ あと30点 / 順調に移動中            │
└──────────────────────────────┘
```

- ステージ名と累積スコアは常に表示します。
- 通常ルートでは現在地、次の目的地、残りスコアを表示します。
- 最後の目的地以降は探索レベル、進捗、次のレベルまでの残りスコアを表示します。
- 動きが停止していても、テキストだけで現在状態を理解できるようにします。

## 11. ビジュアルと音の方針

- 旅アニメーション領域の背景は白、線と塗りは黒を使用し、白黒だけのピクトグラム風SVGで構成します。累積スコア領域は既存の緑系デザインを維持します。
- 中間色が必要な場合は別の色を追加せず、黒の透明度や線密度で表現します。
- 装飾的な丸みを抑え、直線・グリッド・幾何学形状を中心とした無機質な造形にします。
- ステージの違いは車・飛行機・宇宙船と、それぞれの背景構造で表現します。
- スコアパネルの上下領域は、旅アニメーション3、累積スコア2の高さ比で配置します。
- SVGの座標系は横3：縦2とし、表示領域に合わせて縦横比を維持したまま拡大縮小します。
- 自動再生音は使用しません。
- 到着効果音を追加する場合も、初期値はオフとします。

## 12. 保存と再読み込み

以下を採点セッションIDへ紐づけて保存します。

- 選択された日本ルート
- 選択された世界ルート
- 累積獲得スコアの算出元となる完了区間
- 直近の完了区間IDと獲得スコアは、保存済みの完了区間から復元します。

現在ステージ、目的地、探索レベルは累積獲得スコアから再計算できるため、重複保存しません。

再読み込み後も同じルート、ステージ、乗り物の状態を復元します。CSSアニメーションの途中時刻は復元せず、現在状態の先頭フレームから再開します。

## 13. アクセシビリティ

- `prefers-reduced-motion: reduce`では移動・回転・点滅を停止します。
- 動きを止めても、現在地、状態、残りスコアをテキスト表示します。
- 1分区間の確定は画面表示だけを更新し、読み上げを繰り返しません。
- 目的地到着とステージ到達だけを`aria-live="polite"`で通知します。
- 秒単位の背景移動は読み上げ対象にしません。
- 装飾用SVGは`aria-hidden="true"`とします。
- ステージ遷移演出にはスキップ操作を用意します。

## 14. 実装単位

| 実装 | 役割 |
|---|---|
| 旅進行ロジック | 累積値からステージ・現在地・ミッション番号を計算 |
| 状態変換ロジック | タイマー状態と直近獲得点から`MotionState`を計算 |
| ステージ設定 | 背景、乗り物、ラベル、境界値を定義 |
| SVG部品 | 車、飛行機、宇宙船、背景、小物を描画 |
| アニメーション制御 | 動きの変更、到着、ステージ遷移を再生 |
| 保存処理 | 選択ルートと最後に表示したイベントを保存 |

ステージや状態の判定は純粋関数として実装し、Reactコンポーネント内へ境界値を直接記述しません。

## 15. 最低限のテスト項目

- 359点は日本、360点は世界になること
- 899点は世界、900点は宇宙になること
- 1,305点以降も宇宙ステージが継続すること
- 1,305点は深宇宙ミッション1、1,350点はミッション2になること
- 0～3点で乗り物自体は変わらず、動きだけが変わること
- タイマーモードが直近スコアの状態より優先されること
- 休憩・離席で累積スコアが増えないこと
- 未完了区間で表示状態が変わらないこと
- 再描画や再読み込みで到着演出を二重再生しないこと
- モーション軽減設定でアニメーションが停止すること

## 16. 初期実装の範囲

### 対象

- 日本・世界・宇宙の3ステージ判定
- 各ステージ1種類の乗り物
- `score0`～`score3`、休憩、停止（離席を含む）の動き
- 30点ごとの目的地更新
- 日本から世界、世界から宇宙への遷移演出
- 宇宙ステージの探索レベル表示
- モーション軽減設定

### 対象外

- 月面探索と探索レベル
- 惑星への着陸・コロニー建設
- 乗り物のカスタマイズ
- 音声・効果音
- 複数の宇宙ルート選択
- ランキングや他ユーザーとの競争

## 17. 現在の実装との差分

- 現在の`RouteArea`はすでに日本・世界・宇宙の3種類です。
- 690点の「月周回軌道」到着後は、30点ごとに探索レベルを更新します。
- 現在の表示は進捗点と簡易トラベラーのみのため、ステージ別のSVGと`MotionState`を追加します。
- 現在の表示はタイマーモードと直近獲得スコアを受け取らないため、表示用の状態変換を追加します。
- 選択ルートはメモリ上にあるため、再読み込み後も同じルートになるよう保存します。
- 到着済みイベントを記録し、強調演出の二重再生を防ぎます。

実装は、旅ロジック、静止SVG、状態別モーション、到着・ステージ遷移の順に進めます。
