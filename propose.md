# 移動スコアと旅アニメーションの実装計画

## 1. 目的

3分ごとに算出される0〜100点の集中スコアを、作業中のキャラクターの状態と旅の進行へ反映する。

高得点を取り続けるために画面を確認する行為が集中の妨げにならないよう、集中スコアは3段階の状態へ丸める。41〜100点では見た目と進行量を変えず、一定以上集中できていれば同じように進む設計とする。

旅は累積した移動スコアに応じて「日本 → 世界 → 宇宙 → 月面」へ進み、月面到着後も走行距離を蓄積できるようにする。

## 2. 用語の整理

| 名称 | 意味 | 値の範囲・単位 |
| --- | --- | --- |
| 集中スコア | 姿勢・安定性・検出状態から3分ごとに算出する評価値 | 0〜100点 |
| 移動状態 | 集中スコアを画面演出用に3段階へ分類した状態 | ガス欠・低効率・通常 |
| 獲得移動スコア | 1回の3分評価で旅に加算する値 | 0・1・3点 |
| 累積移動スコア | 過去に獲得した移動スコアの合計 | 0以上 |
| 旅ステージ | 累積移動スコアから決定する現在地 | 日本・世界・宇宙・月面 |

既存画面の「現在のスコア」「元のスコア」「加算スコア」は意味が混ざりやすいため、実装時に上記の名称へ統一する。

## 3. 確定するルール

### 3.1 3分ごとの移動状態と加算値

| 集中スコア | 移動状態 | 表示 | 獲得移動スコア |
| ---: | --- | --- | ---: |
| 0 | ガス欠 | 停止・休憩している | 0 |
| 1〜40 | 低効率 | ゆっくり動く | 1 |
| 41〜100 | 通常 | 普通に動く | 3 |

- 41点と100点の獲得量およびアニメーション速度は同じにする。
- 未完了の3分区間には移動スコアを付与しない。
- 集中モード中に完了した3分区間だけを評価対象とする。
- 休憩中・離席中はガス欠表示とし、移動スコアを加算しない。
- 同じ3分区間を再描画や画面遷移によって二重加算しない。

### 3.2 旅の進行

通常状態では3分ごとに3点、1時間で20回評価されるため、1時間あたり60点を獲得する。

| 累積移動スコア | 通常状態での目安 | 旅ステージ | 表現する内容 |
| ---: | ---: | --- | --- |
| 0〜59 | 0〜1時間 | 日本 | 日本一周に向かって進む |
| 60〜179 | 1〜3時間 | 世界 | 日本一周達成後、世界一周に向かって進む |
| 180〜359 | 3〜6時間 | 宇宙 | 世界一周達成後、月へ向かって進む |
| 360以上 | 6時間以上 | 月面 | 月面車で走り続ける |

到達イベントは60点で日本一周、180点で世界一周、360点で月面到着とする。月面走行距離は「累積移動スコア - 360」で算出し、初期表示では1点を1 kmとして扱う。

### 3.3 乗り物と演出

乗り物は集中スコアではなく旅ステージで変化させる。

| 旅ステージ | 乗り物の初期案 |
| --- | --- |
| 日本 | 電車または車 |
| 世界 | 飛行機 |
| 宇宙 | ロケット |
| 月面 | 月面車 |

各乗り物に「停止」「低速」「通常」の3状態を用意する。高得点専用の高速化、点滅、紙吹雪などは計測中に表示しない。ステージ到達演出は3分評価の直後に短く表示するか、結果画面でまとめて表示する。

## 4. データ設計

### 4.1 追加する主な型

`src/features/journey/`を追加し、スコア計算そのものと旅の進行判定を分離する。

| 型 | 主な内容 |
| --- | --- |
| `MovementState` | `outOfFuel`・`inefficient`・`normal` |
| `JourneyStage` | `japan`・`world`・`space`・`moon` |
| `ScoreEvaluation` | 評価ID、評価時刻、3分間の集中スコア、移動状態、獲得移動スコア |
| `JourneyProgress` | 累積移動スコア、現在ステージ、次の到達点、月面走行距離 |

境界値やステージ到達値は`journeyConfig.ts`へまとめ、UI側へ数値を直書きしない。

### 4.2 計算処理

以下の純粋関数を用意する。

1. 集中スコアから移動状態を決定する。
2. 移動状態から獲得移動スコアを決定する。
3. 累積移動スコアから旅ステージと次の到達点までの残りを決定する。
4. 評価結果を既存の累積値へ一度だけ反映する。

0未満や100を超える集中スコアは、呼び出し元の不具合を隠さないよう入力エラーとして扱う。画面表示時のフォールバックは別途UI側で用意する。

### 4.3 保存

初期実装では既存方針に合わせて`sessionStorage`へ以下を保存する。

- 累積移動スコア
- 最後に反映した評価ID
- 現在のセッションで獲得した移動スコア
- 到達済みステージ

保存データにはバージョン番号を持たせ、破損または未対応バージョンの場合は安全に初期値へ戻す。旅をブラウザ終了後や別日にも継続する要件を追加する際は、保存先をIndexedDBへ移行する。

## 5. 画面への反映

### 5.1 計測画面

現在のスコア表示エリアを旅の進行が分かる構成へ変更する。

- 現在の集中スコア（0〜100）
- 「ガス欠」「低効率」「通常」の状態ラベル
- 現在の乗り物と控えめな移動アニメーション
- 累積移動スコア
- 現在の旅ステージ
- 次の到達地点までの残りスコアと進捗バー

進捗バーは現在ステージ内の進行率を表示する。例えば累積120点なら、世界ステージの60〜180点のうち50%として扱う。

### 5.2 結果画面

計測終了時に以下を表示する。

- 今回の集中スコア
- 今回獲得した移動スコア
- 更新後の累積移動スコア
- 現在地または新しく到達した地点
- 次の到達地点までの残りスコア
- 月面到着後は月面走行距離

新しい到達地点をまたいだ場合だけ達成メッセージを表示する。通常の高得点だけでは追加演出を出さない。

### 5.3 アクセシビリティ

- 状態を色や動きだけで伝えず、テキストラベルを併記する。
- `prefers-reduced-motion`が有効な場合は移動アニメーションを停止し、静止画へ切り替える。
- 3分ごとの更新は`aria-live`で読み上げるが、秒単位の変化は読み上げない。
- ガス欠を失敗扱いの強い警告色にせず、休憩・離席を自然な状態として表現する。

## 6. 実装手順

### Phase 1: 旅ロジック

1. `src/features/journey/`に型、設定値、変換関数を追加する。
2. 集中スコアの3段階分類と移動スコア計算を実装する。
3. 累積値から旅ステージ、進捗率、残りスコア、月面走行距離を算出する。
4. 境界値の単体テストを追加する。

### Phase 2: 3分評価との接続

1. 集中中の計測データを3分単位で確定する処理を用意する。
2. 確定した`ScoreResult.totalScore`を旅ロジックへ渡す。
3. 評価IDを発行し、二重加算を防止する。
4. 休憩・離席区間を加算対象から除外する。
5. セッション終了時に3分未満の端数を加算しないことを確認する。

### Phase 3: 状態管理と保存

1. `App.tsx`のプレビュー用スコアを実データへ置き換える。
2. 集中スコア、今回獲得値、累積移動スコアを別々の状態として管理する。
3. `sessionStorage`への読込・保存・復旧処理を追加する。
4. スタート、計測、結果の画面遷移後も値が維持されることを確認する。

### Phase 4: UIとアニメーション

1. 旅の状態を表示する共通コンポーネントを追加する。
2. 計測画面へ現在状態、乗り物、進捗を組み込む。
3. 結果画面へ今回獲得値と到達地点を組み込む。
4. 日本・世界・宇宙・月面の背景と乗り物素材を追加する。
5. 3状態のアニメーションと動きを抑える設定を追加する。

### Phase 5: 結合確認

1. 実際の3分計測で評価が1回だけ加算されることを確認する。
2. タイマーの停止、休憩、離席、再開をまたいでも加算が崩れないことを確認する。
3. 各到達点をまたぐテストデータで背景と乗り物が切り替わることを確認する。
4. リロードと画面遷移後の復元を確認する。
5. `npm run test`、`npm run lint`、`npm run build`を実行する。

## 7. テスト項目

### 単体テスト

- 集中スコア0、1、40、41、100の分類結果
- 集中スコア0、1、40、41、100の獲得移動スコア
- 累積移動スコア0、59、60、179、180、359、360の旅ステージ
- 月面到着時と到着後の月面走行距離
- 20回の通常評価で60点になること
- 不正な集中スコアと破損した保存データの処理

### 結合テスト

- 3分経過前には加算されないこと
- 3分経過時に一度だけ加算されること
- 41点と100点で同じ進行量とアニメーションになること
- 休憩中と離席中には加算されないこと
- 画面遷移や再描画で二重加算されないこと
- ステージ到達を一度だけ通知すること

## 8. 完了条件

- 3分ごとの集中スコアが3段階へ正しく分類される。
- 通常状態では1時間で60点、3時間で180点、6時間で360点を獲得できる。
- 60点で日本一周、180点で世界一周、360点で月面到着が表示される。
- 41〜100点では進行量と計測中の演出に差がない。
- 休憩・離席・未完了区間では移動スコアが増えない。
- 累積値が画面遷移とリロード後も維持される。
- 月面到着後も走行距離を継続して記録できる。
- 単体テスト、Lint、ビルドがすべて成功する。

## 9. 今回の対象外

- 高得点専用の演出やランキング
- サーバー同期、アカウント間同期、他端末との共有
- 実距離・実際の乗り物速度に基づくシミュレーション
- 月面より先の惑星や追加ステージ
- 乗り物素材の最終デザイン決定
