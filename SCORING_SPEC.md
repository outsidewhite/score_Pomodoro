# スコア計測仕様と現在の実装ロジック

## 1. この文書の目的

本書は、スコア計測に関する仕様変更と、現在の実装ロジックを共有するための資料です。

主に以下を説明します。

- 1分区間の評価方法
- 基準姿勢の取得条件
- 姿勢・安定性・検出状態の採点方法
- 獲得スコアへの変換
- 離席判定とタイマー停止
- 完了区間の保存と最終総合評価
- 通知が表示されるタイミング

## 2. 主な仕様変更

| 項目 | 変更前 | 現在の仕様 |
|---|---|---|
| 評価区間 | 3分単位 | 実時間60秒単位 |
| 評価間隔 | 200ms | 500ms |
| 1区間の評価回数 | 可変 | 最大120回 |
| 基準姿勢 | 固定的な姿勢判定 | 最初に条件を満たした1分間の肩位置平均 |
| 基準姿勢の確定条件 | 未確定 | 120回中108回以上で人物を検出 |
| 基準姿勢取得中の重み | 通常区間と同じ | 安定性60%・検出状態40% |
| 通常区間の重み | 姿勢40%・安定性40%・検出状態20% | 維持 |
| 検出状態 | フレームのvisibility平均 | 15秒ごとに10回以上検出できれば25点 |
| 離席判定 | 手動停止が中心 | 検出0点が2区間連続した場合に自動停止 |
| 区間データ | 集計値のみ | 完了した1分区間ごとに内部保存 |
| 再読み込み | スコアを初期化 | `sessionStorage`から完了区間と基準姿勢を復元 |

## 3. 計測の基本単位

計測画面へ遷移した際に姿勢解析モデルを一度だけ読み込みます。読み込みが完了するまで集中タイマーは開始できません。停止・休憩・再開では読み込み済みのモデルを再利用します。

モデル準備完了後、集中タイマーが開始された時刻とスコア区間の開始時刻を同期します。

```text
計測画面へ遷移
  ↓
モデル読み込み（タイマー開始不可）
  ↓
モデル準備完了（タイマー開始可能）
  ↓
集中開始
  ↓
500msごとに評価
  ↓
15秒ごとに検出状態・安定性を判定
  ↓
60秒で区間スコアを確定
  ↓
区間結果を保存して次の60秒を開始
```

- 1区間は実時間60秒です。
- 500msごとに評価し、1区間あたり最大120回記録します。
- 15秒ごとの4区間に分けて、検出状態と安定性を採点します。
- タイマー停止、休憩、離席へ切り替わった場合、進行中の未完了区間は破棄します。
- 集中へ戻った場合は、前回の端数を引き継がず、新しい60秒区間を開始します。
- 120回に満たない区間は確定・保存・加算できません。

### 3.1 評価タイミングが遅延した場合

端末負荷やバックグラウンド化などにより500msごとの処理が遅れた場合、過ぎた評価タイミングを`missed`として0点で記録します。

遅延後に取得した1つの映像フレームを、過去の複数タイミングへ重複使用することはありません。

## 4. フレーム評価状態

各評価タイミングは、以下のいずれかに分類します。

| 状態 | 意味 | 採点上の扱い | 離席判定 |
|---|---|---|---|
| `detected` | 正常に解析し、左右の肩を検出できた | 各指標を計算 | 検出あり |
| `absent` | 解析は成功したが人物を検出できなかった | 0点 | 判定対象 |
| `missed` | 評価タイミングで新しい映像を取得できなかった | 0点 | 判定材料にしない |
| `failed` | MediaPipeなどの解析処理自体が失敗した | 集計の分母から除外 | 判定材料にしない |

`detected`とするには、MediaPipeの左肩と右肩が両方必要です。

- 左肩ランドマーク：11
- 右肩ランドマーク：12
- 両肩の`visibility`：それぞれ0.5以上
- X・Y座標：有限の数値
- 左右の肩幅：0より大きい

## 5. 基準姿勢

基準姿勢が未確定の場合、完了した60秒区間を基準姿勢取得区間として扱います。

### 5.1 確定条件

120回の評価予定タイミングのうち、108回以上で左右の肩を正常検出できた場合に確定します。

```text
必要検出数 = 120 × 90% = 108回
```

処理失敗を分母から外して検出率を上げることはせず、実際に108回以上検出できたことを条件とします。

### 5.2 保存する値

検出に成功したフレームから、以下の平均値を作成します。

```ts
type PostureBaseline = {
  centerX: number
  centerY: number
  shoulderAngle: number
  shoulderWidth: number
}
```

- `centerX`：左右の肩の中点X座標
- `centerY`：左右の肩の中点Y座標
- `shoulderAngle`：左右の肩を結ぶ線の角度
- `shoulderWidth`：左右の肩の距離

座標にはMediaPipeの正規化座標を使用します。肩角度は180度周期の円周平均で算出し、角度境界をまたぐデータも平均できるようにします。

### 5.3 基準姿勢を確定できなかった場合

- 基準姿勢は保存しません。
- 姿勢スコアは`null`として記録します。
- 安定性60%・検出状態40%で区間総合スコアを算出します。
- その区間も最終総合評価と獲得スコアへ含めます。
- 次の60秒区間で基準姿勢の取得を再試行します。
- 取得失敗を画面右上へ通知します。

## 6. 姿勢スコア

基準姿勢確定後は、各`detected`フレームについて肩中点のずれを計算します。

```text
ずれ率 =
  現在の肩中点と基準姿勢の肩中点との距離
  ÷ 基準姿勢の肩幅
```

| ずれ率 | フレーム姿勢スコア |
|---|---:|
| 10%以下 | 100点 |
| 10%超～50%未満 | 100点から0点まで線形減少 |
| 50%以上 | 0点 |

60秒区間内のフレーム姿勢スコアを平均し、区間の姿勢スコアとします。

- `absent`と`missed`は0点として平均へ含めます。
- `failed`は平均の分母から除外します。
- 基準姿勢取得区間では姿勢スコアを算出しません。

## 7. 安定性スコア

安定性は15秒ごとに採点し、4区間の合計を0～100点として扱います。

各15秒区間で、検出できた肩角度の最大値と最小値の差を求めます。

| 肩角度の変動幅 | 15秒区間の基礎点 |
|---|---:|
| 0°以上10°以下 | 25点 |
| 10°超35°未満 | 25点から1点まで線形減少 |
| 35°以上 | 0点 |

10°超35°未満では、以下の式を使用します。

```text
25 - ((角度差 - 10) ÷ 25) × 24
```

さらに、未検出と評価欠落を0点として反映します。

```text
15秒区間の安定性 =
  角度変動の基礎点
  × 検出フレーム数
  ÷ failedを除いた評価数
```

- 検出できた肩角度が2件未満の場合は0点です。
- `failed`は分母から除外します。
- 内部では小数を保持し、画面表示時に四捨五入します。

## 8. 検出状態スコア

60秒を15秒ごとの4区間に分けて採点します。

- 1つの15秒区間には評価予定タイミングが30回あります。
- 30回中10回以上で人物を検出できた場合は25点です。
- 10回未満の場合は0点です。
- 4区間の合計を検出状態スコアとします。
- 最小0点、最大100点です。

## 9. 区間総合スコア

すべての指標を0～100点として重み付けします。

### 9.1 基準姿勢取得中

```text
区間総合スコア = 安定性 × 0.6 + 検出状態 × 0.4
```

### 9.2 基準姿勢確定後

```text
区間総合スコア =
  姿勢 × 0.4
  + 安定性 × 0.4
  + 検出状態 × 0.2
```

内部計算では小数を保持します。

## 10. 獲得スコア

区間総合スコアを、旅や累積表示で使用する獲得スコアへ変換します。

| 区間総合スコア | 獲得スコア |
|---:|---:|
| 0点 | 0点 |
| 1～40点 | 1点 |
| 41～80点 | 2点 |
| 81～100点 | 3点 |

区間総合スコアと獲得スコアは別の値として保存します。

## 11. 自動離席判定

15秒区間の検出状態スコアが2回連続で0点になった場合、約30秒の離席と判定します。

ただし、処理失敗や映像欠落だけで誤停止しないよう、`detected`と`absent`の合計が10回以上ある区間だけを離席判定へ使用します。

- 25点の区間：連続0点回数を0へ戻します。
- 判定可能な0点区間：連続0点回数を1増やします。
- `failed`や`missed`が多く判定不能な区間：回数を増減させません。

2回連続した場合は以下を実行します。

1. 進行中の60秒区間を破棄する。
2. 集中タイマーを停止する。
3. タイマーモードを`away`へ変更する。
4. 離席ログを追加する。
5. 連続0点回数をリセットする。
6. 離席通知を1回表示する。

基準姿勢は削除しません。ユーザーが手動で再開すると、新しい60秒区間から計測を開始します。

## 12. 完了区間の内部データ

完了した区間は以下の形式で保持します。

```ts
type ScoreIntervalResult = {
  id: string
  startedAt: number
  endedAt: number
  isCalibration: boolean
  calibrationSucceeded: boolean
  postureScore: number | null
  stabilityScore: number
  detectionScore: number
  totalScore: number
  earnedScore: 0 | 1 | 2 | 3
  detectedCount: number
  absentCount: number
  missedCount: number
  failedCount: number
}
```

区間IDはセッションIDと区間番号から作成します。

```text
${sessionId}:interval:${intervalNumber}
```

同じIDがすでに保存されている場合は追加せず、Reactの再描画などによる二重加算を防止します。

## 13. 保存と再読み込み

保存先には`sessionStorage`を使用します。

```text
採点保存キー: score-pomodoro:scoring-session
タイマー保存キー: score-pomodoro:timer-session
各保存データバージョン: 1
```

保存対象は以下です。

- セッションID
- 完了区間一覧
- 次の区間番号
- 目標作業時間
- セッションログ
- 最後にタイマー状態を観測した時刻
- 保存データのバージョン

再読み込み時には保存データの型と数値を検証します。不正・破損・未対応バージョンの場合は使用せず、新しいセッションとして初期化します。

### 13.1 再読み込み後の動作

- 完了区間と累積獲得スコアは復元します。基準姿勢は復元せず、再開後の最初の区間で較正し直します。
- 未完了区間は復元しません。
- カメラストリーム自体は復元できませんが、計測画面を再読み込みしても開始画面へは戻りません。
- 計測画面を維持したままカメラを自動で再取得し、取得できない場合は画面内の操作から再試行できます。
- カメラとモデルの準備完了後に集中を再開すると、保存済みセッションを引き継いで新しい60秒区間を開始します。
- タイマーの経過時間とセッションログは、同じセッションIDの保存データから復元します。
- 再読み込み直前の集中・休憩ログは最後に観測した時刻で確定し、再開までの時間を離席として記録します。
- 結果画面の「もう一度計測する」を選択した場合、保存データを削除して新しいセッションを作成します。

## 14. 最終総合評価

セッション終了時に、完了したすべての区間総合スコアを平均します。

```text
最終総合評価 =
  完了区間のtotalScore合計
  ÷ 完了区間数
```

- 基準姿勢を確定できなかった区間も含めます。
- 停止・休憩・離席で破棄された未完了区間は含めません。
- 表示時に四捨五入します。
- 完了区間が0件の場合は「評価なし」と表示します。
- 累積獲得スコアとは別の値として表示します。

姿勢スコアの最終表示値は、姿勢スコアが`null`ではない通常区間だけを平均します。通常区間がない場合は0点です。

## 15. 通知タイミング

現在のアプリでは、以下のタイミングで通知を表示します。

| タイミング | 通知 |
|---|---|
| 姿勢解析モデルの読込開始・再読込開始 | 「モデル読み込み中です」 |
| 姿勢解析モデルの読込成功 | 「読み込みに成功しました！」 |
| 姿勢解析モデルの読込失敗 | エラー詳細と「再読み込み」操作 |
| 映像フレームの解析処理失敗 | 姿勢解析エラーと詳細 |
| 基準姿勢取得区間の完了時に108回未満 | 次の1分で再試行する旨を通知 |
| 基準姿勢取得区間の完了時に108回以上 | 基準姿勢を保存した旨を通知 |
| 検出状態0点が2区間連続 | 離席を検出してタイマーを停止した旨を通知 |
| 計測中に映像トラックが終了 | カメラ切断を通知し、タイマーと姿勢解析を停止 |

通知は画面右上に表示し、標準では6秒後に閉じます。モデルの読み込み中・失敗通知は、結果が確定するかユーザーが操作するまで表示します。同じ種類の通知は積み重ねず、既存の通知を更新します。

通常の1分区間完了、獲得スコア更新、タイマーの手動開始・停止、休憩開始、セッション終了では通知しません。

## 16. 実装箇所

| 役割 | ファイル |
|---|---|
| 500ms評価と60秒区間の制御 | `src/features/pose/usePoseScoring.ts` |
| 基準姿勢・各指標・獲得点の計算 | `src/features/scoring/intervalScoring.ts` |
| セッション保存・復元・最終集計 | `src/features/scoring/scoringSession.ts` |
| カメラ切断の検知と通知 | `src/features/camera/cameraStreamMonitor.ts`、`src/features/camera/useCameraDisconnect.ts` |
| 自動離席と通知の接続 | `src/pages/measurement/MeasurementPage.tsx` |
| タイマー停止と離席ログ | `src/components/Timer/Timer.tsx` |
| セッション全体の状態管理 | `src/App.tsx` |
| 結果表示 | `src/pages/result/ResultPage.tsx` |

## 17. 現在の注意点

- MediaPipeモデルとWASMは外部URLから読み込むため、初回起動時はネットワーク状態の影響を受けます。
- モデルを読み込めない場合、集中タイマーは開始できません。失敗通知の「再読み込み」からユーザー操作で再試行できます。
- 肩が画面外に出る、暗い、身体の一部が隠れるなどの場合は検出回数が低下します。
- 基準姿勢確定には実際の検出が108回必要です。モデルの準備時間は計測区間へ含めませんが、映像未準備や肩の未検出は検出回数へ影響します。
- タブを閉じるまでの復元には`sessionStorage`を使用しています。ブラウザを閉じた後や別日の継続保存は現在の対象外です。
- 実カメラ環境では、検出閾値や姿勢ずれ率の閾値を今後調整する可能性があります。
